name: alfred_app
description: "ALFRED"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_litert_lm:
    path: local_plugins/flutter_litert_lm
  dio: ^5.4.0
  path_provider: ^2.1.1
  record: ^7.0.0
  flutter_markdown: ^0.7.7+1
  web_socket_channel: ^3.0.3
  crypto: ^3.0.7
  audioplayers: ^6.8.1
  sherpa_onnx: ^1.13.5
  flutter_background_service: ^5.1.0
  flutter_local_notifications: ^22.3.0
  archive: ^4.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/tts/fr_FR-tom-medium.onnx
    - assets/tts/tokens.txt
    - assets/tts/espeak-ng-data.zip